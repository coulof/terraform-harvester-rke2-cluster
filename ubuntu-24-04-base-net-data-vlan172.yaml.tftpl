version: 2
ethernets:
  enp1s0:
    dhcp4: false
    dhcp6: false
    addresses:
      - ${ip_address}
    routes:
      - to: default
        via: ${gateway}
    nameservers:
      addresses: ${jsonencode(dns_servers)}
      search: [${domain}]
